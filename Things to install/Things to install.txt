git
noisetorch
snapper
sudo dnf install https://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
sudo dnf install https://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm

disable other repo
sudo dnf5 config-manager setopt nextdns.enabled=0

sudo dnf install mesa-va-drivers mesa-vdpau-drivers libva libva-utils vdpauinfo

nerd fond jetbrain

remove kde mail and other things

mpv

brave-browser

zsh startship

python pyenv

cargo

vscode npm

syncthing

miniserve

obsidian

qbit-torrent

tailscale



INSIDE UBUNTU DEVELOPMENT PACKAGES:
sudo apt update && sudo apt install -y \
    build-essential \
    binutils \
    cmake \
    ninja-build \
    pkg-config \
    libtool \
    autoconf \
    automake \
    meson \
    gdb \
    strace \
    ltrace \
    valgrind \
    perf \
    htop \
    lsof \
    zlib1g-dev \
    libssl-dev \
    libffi-dev \
    libbz2-dev \
    liblzma-dev \
    libreadline-dev \
    libsqlite3-dev \
    uuid-dev \
    libncurses-dev \
    libxml2-dev \
    libxslt1-dev \
    libevent-dev \
    tk-dev \
    libgdbm-dev \
    libgdbm-compat-dev \
    wget \
    curl

